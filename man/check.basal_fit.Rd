% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check.basal_fit}
\alias{check.basal_fit}
\title{Posterior Predictive and Regression checks from fit \code{basal} model.}
\usage{
check.basal_fit(
  fit,
  stat = c(ecdf = stats::ecdf),
  include_base_pp_check = TRUE,
  draws = 50,
  trace_plots = FALSE,
  two_stage_stat = c(proportion_positive = prop_positive),
  join_two_stage_stat = c(joined_ecdf = stats::ecdf)
)
}
\arguments{
\item{fit}{object of class \code{fit} from \code{fit()}.}

\item{stat}{extra functions to plot for posterior checks. Multiple can be
specified if included in a list (i.e., \code{stat = c(mean = mean, ecdf = ecdf)})
functions should be named.}

\item{include_base_pp_check}{Logical determining whether or not to include the
empirial density funciton as a posterior predictive check. Defaults to \code{TRUE}}

\item{draws}{number of draws for posterior prediction}

\item{trace_plots}{Logical flag to include trace plots of parameter estimates.
Defaults to \code{FALSE} so that large number of plots (if including many random effects)
are not included in the object.}

\item{two_stage_stat}{Like \code{stat}, but used for the second stage of a second-stage model}

\item{join_two_stage_stat}{Like \code{stat} but used for the response aggregated
from the multi-stage model.}
}
\description{
Posterior Predictive and Regression checks from fit \code{basal} model.
}
